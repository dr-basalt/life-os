version: "3.9"

# ── Déploiement o3c — cockpit.infra.ori3com.cloud ─────────────────────────────
# Utilise le Traefik existant (/data/docker-compose.yml)
# Network externe : traefik
# Cert resolver : le (Cloudflare DNS challenge)
#
# Démarrage :
#   cp .env.o3c .env
#   docker compose -f docker-compose.o3c.yml up -d

services:
  # ── PocketBase + Litestream ─────────────────────────────────────────────────
  sternos-pb:
    build: ./pocketbase
    container_name: sternos-pb
    restart: unless-stopped
    environment:
      S3_ENDPOINT: ${S3_ENDPOINT}
      S3_BUCKET: ${S3_BUCKET}
      S3_REGION: ${S3_REGION:-auto}
      LITESTREAM_ACCESS_KEY_ID: ${S3_KEY}
      LITESTREAM_SECRET_ACCESS_KEY: ${S3_SECRET}
      PB_ADMIN_EMAIL: ${PB_ADMIN_EMAIL}
      PB_ADMIN_PASSWORD: ${PB_ADMIN_PASSWORD}
    volumes:
      - sternos_pb_data:/pb/pb_data
    networks:
      - traefik
      - sternos_internal
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik"
      # API PocketBase
      - "traefik.http.routers.sternos-api.rule=Host(`api-cockpit.infra.ori3com.cloud`)"
      - "traefik.http.routers.sternos-api.entrypoints=websecure"
      - "traefik.http.routers.sternos-api.tls.certresolver=le"
      - "traefik.http.services.sternos-api.loadbalancer.server.port=8090"
      # Admin PocketBase (optionnel — à désactiver en prod si non nécessaire)
      - "traefik.http.routers.sternos-admin.rule=Host(`pb-cockpit.infra.ori3com.cloud`)"
      - "traefik.http.routers.sternos-admin.entrypoints=websecure"
      - "traefik.http.routers.sternos-admin.tls.certresolver=le"
      - "traefik.http.services.sternos-admin.loadbalancer.server.port=8090"
      # Middleware basic auth sur l'admin PB
      - "traefik.http.routers.sternos-admin.middlewares=sternos-auth"
      - "traefik.http.middlewares.sternos-auth.basicauth.users=${PB_BASIC_AUTH}"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8090/api/health"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 45s

  # ── SvelteKit Frontend ──────────────────────────────────────────────────────
  sternos-frontend:
    build:
      context: ./frontend
      args:
        PUBLIC_PB_URL: https://api-cockpit.infra.ori3com.cloud
    container_name: sternos-frontend
    restart: unless-stopped
    environment:
      ORIGIN: https://cockpit.infra.ori3com.cloud
      PUBLIC_PB_URL: https://api-cockpit.infra.ori3com.cloud
    depends_on:
      sternos-pb:
        condition: service_healthy
    networks:
      - traefik
      - sternos_internal
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik"
      - "traefik.http.routers.sternos-app.rule=Host(`cockpit.infra.ori3com.cloud`)"
      - "traefik.http.routers.sternos-app.entrypoints=websecure"
      - "traefik.http.routers.sternos-app.tls.certresolver=le"
      - "traefik.http.services.sternos-app.loadbalancer.server.port=3000"

  # ── DB interne Windmill (optionnel — si tu veux Windmill en plus de n8n) ───
  # Sinon : pointe tes agents sur n8n1890.infra.ori3com.cloud (déjà running)
  #
  # sternos-windmill:
  #   image: ghcr.io/windmill-labs/windmill:main
  #   ...

volumes:
  sternos_pb_data:
    name: sternos_pb_data

networks:
  traefik:
    external: true        # réseau Traefik existant
  sternos_internal:
    name: sternos_internal # réseau privé inter-services
