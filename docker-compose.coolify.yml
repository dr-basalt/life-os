version: "3.9"

# ── Déploiement via Coolify sur taz-jarvis-hetzner ────────────────────────────
# Proxy : Traefik (réseau externe 'coolify')
# FQDNs :
#   cockpit.infra.ori3com.cloud       → frontend:3000
#   api-cockpit.infra.ori3com.cloud   → lifeos-pb:8090

services:
  # ── PocketBase ───────────────────────────────────────────────────────────────
  lifeos-pb:
    build: ./pocketbase
    restart: unless-stopped
    environment:
      S3_ENDPOINT: ${S3_ENDPOINT:-}
      S3_BUCKET: ${S3_BUCKET:-}
      S3_REGION: ${S3_REGION:-auto}
      LITESTREAM_ACCESS_KEY_ID: ${S3_KEY:-}
      LITESTREAM_SECRET_ACCESS_KEY: ${S3_SECRET:-}
      PB_ADMIN_EMAIL: ${PB_ADMIN_EMAIL}
      PB_ADMIN_PASSWORD: ${PB_ADMIN_PASSWORD}
    volumes:
      - lifeos_pb_data:/pb/pb_data
    networks:
      - coolify
      - lifeos_internal
    expose:
      - "8090"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.lifeos-api.rule=Host(`api-cockpit.infra.ori3com.cloud`)"
      - "traefik.http.routers.lifeos-api.entrypoints=https"
      - "traefik.http.routers.lifeos-api.tls=true"
      - "traefik.http.routers.lifeos-api.tls.certresolver=letsencrypt"
      - "traefik.http.services.lifeos-api.loadbalancer.server.port=8090"

  # ── SvelteKit Frontend ──────────────────────────────────────────────────────
  lifeos-frontend:
    build:
      context: ./frontend
      args:
        PUBLIC_PB_URL: ${PUBLIC_PB_URL:-https://api-cockpit.infra.ori3com.cloud}
    restart: unless-stopped
    environment:
      ORIGIN: ${ORIGIN:-https://cockpit.infra.ori3com.cloud}
      PUBLIC_PB_URL: ${PUBLIC_PB_URL:-https://api-cockpit.infra.ori3com.cloud}
    networks:
      - coolify
      - lifeos_internal
    expose:
      - "3000"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.lifeos-app-http.rule=Host(`cockpit.infra.ori3com.cloud`)"
      - "traefik.http.routers.lifeos-app-http.entrypoints=http"
      - "traefik.http.routers.lifeos-app-http.middlewares=redirect-to-https"
      - "traefik.http.routers.lifeos-app.rule=Host(`cockpit.infra.ori3com.cloud`)"
      - "traefik.http.routers.lifeos-app.entrypoints=https"
      - "traefik.http.routers.lifeos-app.tls=true"
      - "traefik.http.routers.lifeos-app.tls.certresolver=letsencrypt"
      - "traefik.http.services.lifeos-app.loadbalancer.server.port=3000"

volumes:
  lifeos_pb_data:
    name: lifeos_pb_data

networks:
  coolify:
    external: true
  lifeos_internal:
    name: lifeos_internal
